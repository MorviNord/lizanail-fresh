export interface ListTextLink {
  id: number;
  label: string;
  link: string;
}

export interface ListImgLink {
  name: unknown;
  id: number;
  img: string;
  label: string;
}

export interface ListImg {
  id: number;
  img: string;
  link: string;
}
